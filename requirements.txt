# Python >= 3.10
# Pin lightly; exact patch versions are not required for reproducibility because
# all scientific parameters are fixed in code and intermediate data is cached.
numpy>=1.24
scipy>=1.10
numba>=0.58
matplotlib>=3.7
h5py>=3.8
tqdm>=4.65
